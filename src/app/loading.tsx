export default function Loading() {
  return (
    <main>
      <section className="hero">
        <p className="eyebrow">Loading</p>
        <h1 className="title">Preparing the workspace</h1>
        <p className="lede">
          We’re fetching the next view. The MVP skeleton uses lightweight loading states so the
          flow feels responsive even before analysis is implemented.
        </p>
      </section>
    </main>
  );
}

